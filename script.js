const header=document.getElementById("siteHeader"),menuToggle=document.getElementById("menuToggle"),mainNav=document.getElementById("mainNav");function setHeader(){header?.classList.toggle("scrolled",window.scrollY>20)}setHeader();window.addEventListener("scroll",setHeader,{passive:true});menuToggle?.addEventListener("click",()=>{const open=mainNav?.classList.toggle("open");menuToggle.setAttribute("aria-expanded",String(Boolean(open)))});mainNav?.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{mainNav.classList.remove("open");menuToggle?.setAttribute("aria-expanded","false")}));document.querySelectorAll("#year").forEach(y=>y.textContent=String(new Date().getFullYear()));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");observer.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));